{
  "name": "profitpilot",
  "private": true,
  "scripts": {
    "fe": "npm --prefix frontend run dev",
    "be": "cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "concurrently -n FE,BE -c cyan,green \"npm run fe\" \"npm run be\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
